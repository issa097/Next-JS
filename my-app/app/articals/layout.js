export const metadata = {
    title: "articales page"
}
export default function ({ children }) {
    return (
        <div>
            articalsj

            <div>
                {children}
            </div>
        </div>

    )
}